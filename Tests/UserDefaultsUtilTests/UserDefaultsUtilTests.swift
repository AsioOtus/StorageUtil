import XCTest
@testable import UserDefaultsUtil

final class UserDefaultsUtilTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//		XCTAssertEqual(UserDefaultsUtil.text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
